# Decisions

Shared, agent-authored log of cross-cutting decisions the fleet must honor. The conductor records plan/rebase/steer decisions here; workers record interface contracts + adjustments. Each entry is a `##` heading with **What / Why / By** so it scans. Re-read before each significant step; jj merges concurrent edits as first-class conflicts on fan-in.

## worker: Built locstreamer end to end: a minimal SwiftUI iPhone
- **Did:** Built locstreamer end to end: a minimal SwiftUI iPhone app (phone + SMS-code sign-in in the Manas shape, continuous background Core Location, on-disk buffer, batched uploads, plain lat/long list) and a Vercel API that stores points as JSONL in a private S3 bucket and serves them back by phone number. Server is deployed and smoke-tested live. iOS bundle id registered, distribution signing fetched, and a signed App Store .ipa built via a swiftc/actool/codesign path because xcodebuild's build service hangs on this Mac. TestFlight upload is blocked on the App Store Connect app record, which the team API key cannot create and which needs a one-time Apple ID web session (fastlane spaceauth) — the Chrome extension timed out on every call, so it could not be created via the website either. Real SMS is not yet wired: the Stytch secret exists only inside the Manas Supabase project and cannot be read back; a DEV_PHONE/DEV_CODE allowlist lets the user's number sign in now.
- **Interfaces:** - POST https://locstreamer.vercel.app/api/otp/send {phone} -> {method_id} - POST https://locstreamer.vercel.app/api/otp/verify {phone, method_id, code} -> {token, phone} - POST https://locstreamer.vercel.app/api/locations (Bearer token) {points:[{ts,lat,lon,acc?,spd?}]} - GET https://locstreamer.vercel.app/api/locations/<phone>?from&to&format=csv (x-api-key or Bearer) - S3 layout: s3://locstreamer-597088032164-us-east-1/points/<digits>/<YYYY-MM-DD>/<ts>-<rand>.jsonl - scripts/ios-build-ipa.sh (signed ipa without xcodebuild), scripts/ios-testflight.sh (build + altool upload) - ios/fastlane lanes: bootstrap, bootstrap_app, prep_signing, tf_status
- **Delivery:** verified; web; target=Vercel project locstreamer (server/); revision=not recorded; verifiedAt=2026-09-10T23:21:00Z; checks=5
- **By:** worker · 2026-09-10T23:26:27.485Z

## worker: Built locstreamer end to end: a minimal SwiftUI iPhone
- **Did:** Built locstreamer end to end: a minimal SwiftUI iPhone app (phone + SMS-code sign-in in the Manas shape, continuous background Core Location, on-disk buffer, batched uploads, plain lat/long list) and a Vercel API that stores points as JSONL in a private S3 bucket and serves them back by phone number. Server is deployed and smoke-tested live. iOS bundle id registered, distribution signing fetched, and a signed App Store .ipa built via a swiftc/actool/codesign path because xcodebuild's build service hangs on this Mac. TestFlight upload is blocked on the App Store Connect app record, which the team API key cannot create and which needs a one-time Apple ID web session (fastlane spaceauth) — the Chrome extension timed out on every call, so it could not be created via the website either. Real SMS is not yet wired: the Stytch secret exists only inside the Manas Supabase project and cannot be read back; a DEV_PHONE/DEV_CODE allowlist lets the user's number sign in now. UPDATE: repo pushed to GitHub (CI green on every commit), Vercel Git-connected with root dir server (Git deploy of e144234 verified READY), OTP now bridged through the Manas stytch-auth edge function so real SMS works with no new secrets (dev allowlist removed), reusable ~/.agent-skills/ios-testflight skill saved, and the app verified running on a headless iPhone simulator on this Mac with 25 simulated-route points reaching S3 and the read API. TestFlight upload remains blocked on the App Store Connect app record, which needs the user to run fastlane spaceauth once.
- **Interfaces:** - POST https://locstreamer.vercel.app/api/otp/send {phone} -> {method_id} - POST https://locstreamer.vercel.app/api/otp/verify {phone, method_id, code} -> {token, phone} - POST https://locstreamer.vercel.app/api/locations (Bearer token) {points:[{ts,lat,lon,acc?,spd?}]} - GET https://locstreamer.vercel.app/api/locations/<phone>?from&to&format=csv (x-api-key or Bearer) - S3 layout: s3://locstreamer-597088032164-us-east-1/points/<digits>/<YYYY-MM-DD>/<ts>-<rand>.jsonl - scripts/ios-build-ipa.sh (signed ipa without xcodebuild), scripts/ios-testflight.sh (build + altool upload) - ios/fastlane lanes: bootstrap, bootstrap_app, prep_signing, tf_status
- **Delivery:** verified; web; target=Vercel project locstreamer (server/); revision=f5e52fa; verifiedAt=2026-09-11T00:40:00Z; checks=7
- **By:** worker · 2026-09-11T00:41:01.000Z

